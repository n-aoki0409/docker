<source>
  @type forward
  port 24224
</source>

<match>
  # @type file
  # path /tmp/access

  @type stdout

  <format>
    @type single_value
  </format>

  <buffer>
    @type memory
    chunk_limit_size 32MB
    queued_chunks_limit_size 60
    total_limit_size 2048MB
    flush_mode interval
    flush_interval 1s
  </buffer>
</match>
